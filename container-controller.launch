#!/bin/bash

# Sample script for launching the ubuntu container

docker kill ubuntu
docker rm ubuntu
docker run -d -t -p 80:80 -v /var/run/ubuntu-control:/var/run/container-control --name ubuntu tragus/ubuntu
